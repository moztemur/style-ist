type BlushLocations = {
  leftCheek: { x: number; y: number }[];
  rightCheek: { x: number; y: number }[];
};

type MustacheLocations = {
  upperLip: { x: number; y: number }[];
  noseBottomLine: { x: number; y: number }[];
};
